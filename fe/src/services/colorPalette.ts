// utils/colorPalette.ts

export const generateColorPalette = (primaryColor: string, secondaryColor: string): string[] => {
    const colors = [primaryColor, secondaryColor];
    // Añadimos más colores a la paleta (puedes personalizar estos colores)
    colors.push('#FF6384', '#36A2EB', '#FFCE56', '#4BC0C0', '#9966FF', '#FF9F40', '#FF6384', '#36A2EB');
    return colors;
  };
  